<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usedcar.mapper.ViewEventMapper">

    <insert id="insert" parameterType="com.usedcar.domain.ViewEvent"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO view_events (session_id, user_id, car_id, created_at)
        VALUES (#{sessionId}, #{userId}, #{carId}, NOW())
    </insert>

    <select id="existsRecent" resultType="int">
        SELECT COUNT(*) FROM view_events
        WHERE session_id = #{sessionId} AND car_id = #{carId} AND created_at >= #{since}
    </select>

    <select id="findRecentCarIdsBySession" resultType="java.lang.Long">
        SELECT car_id FROM view_events
        WHERE session_id = #{sessionId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="countByCarSince" resultType="map">
        SELECT car_id AS carId, COUNT(*) AS viewCount
        FROM view_events
        WHERE created_at >= #{since}
        GROUP BY car_id
        ORDER BY viewCount DESC
        LIMIT #{limit}
    </select>
</mapper>
