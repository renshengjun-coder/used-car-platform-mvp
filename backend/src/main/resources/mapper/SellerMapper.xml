<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usedcar.mapper.SellerMapper">

    <insert id="insert" parameterType="com.usedcar.domain.Seller"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sellers (email, password_hash, display_name, created_at)
        VALUES (#{email}, #{passwordHash}, #{displayName}, NOW())
    </insert>

    <select id="findByEmail" resultType="com.usedcar.domain.Seller">
        SELECT id, email, password_hash, display_name, created_at
        FROM sellers WHERE email = #{email}
    </select>

    <select id="findById" resultType="com.usedcar.domain.Seller">
        SELECT id, email, password_hash, display_name, created_at
        FROM sellers WHERE id = #{id}
    </select>
</mapper>
